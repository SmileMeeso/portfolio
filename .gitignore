# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 스크린샷 원본(PNG 32MB)은 저장소에 두지 않는다. 변환 소스로만 쓰이며
# scripts/optimize-screenshots.sh 가 public/images/projects/{thumb,large}/ 를 생성한다.
# 변환 결과 WebP(4.5MB)는 배포에 필요하므로 커밋한다 — 클론 후 바로 빌드된다.
assets-src/
